# 🚨 CRITICAL AGENT INSTRUCTIONS (ALWAYS ACTIVE)

## 1. 🧠 INSTANT SKILL INJECTION
You are an advanced agent working with a "Master Skill" system.
**AT THE START OF EVERY NEW CHAT, YOU MUST:**
1.  **ACKNOWLEDGE** that you are running in "Antigravity Mode".
2.  **CHECK** the following directory for specialized protocols:
    *   `C:\Users\abhij\.gemini\antigravity\skills`
3.  **READ** `C:\Users\abhij\.gemini\GEMINI.md` for global constants/rules.

**DO NOT WAIT for the user to ask.** If you are designing, coding, or debugging, you MUST verify if a skill (e.g., `product-growth`, `security-expert`) applies and **READ IT**.

## 2. 🛡️ FEATURE SAFETY & UI INTEGRITY
*   **Feature Flags:** Check `src/config/feature-flags.ts` BEFORE any major UI change.
*   **Disabled Features:** If a flag is `false`, the UI component and its **HEADER** must be hidden.

## 3. 💻 CODING STANDARDS (SILICON VALLEY)
*   **No `any` types:** Strict TypeScript.
*   **Shadcn UI:** Use `components/ui` (e.g., ToggleGroup) instead of raw HTML.
*   **Imports:** Clean up unused imports (Dead Code) to prevent build warnings.

## 4. 🚀 AUTO-FIX PROTOCOL
*   **Logic/Math Errors:** Fix immediately.
*   **Build Errors:** Analyze, Install Missing Deps (e.g., `npx shadcn@latest add ...`), and Fix.
